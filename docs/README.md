# Искра v10/v12 — Монорепозиторий

Единая сборка: ядро (core), UI, web (Next.js), mobile (Expo RN), API (Express/ws), docker.
Включены SLO→рефлексы, протоколы (♲4–7, Rule-21, Gravitas, Ирония), базовая память (TTL+LRU), схемы.

См. `RUN.md` для запуска и `TREE.md` для дерева.
